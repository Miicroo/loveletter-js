class Player {
	constructor(card, gameController) {
		this.card = card;
		this.gameController = gameController;
	}

	play(drawnCard) {
		// Play drawn card or card on hand
		// gameController.play(card, opponent);
	}
}